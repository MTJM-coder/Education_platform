import { useState } from "react";
import { BookOpen, ChevronRight, FileText, X } from "lucide-react";
import AppHeader from "../components/layout/AppHeader";
import { Pill } from "../components/ui/StatusUi";
const resources = [{ id: 1, subject: "Mathématiques", title: "Fractions : comprendre les bases", type: "Fiche de cours", text: "Une fraction représente une ou plusieurs parties égales d’un tout. Le numérateur est le nombre de parties prises, le dénominateur le nombre total de parties." }, { id: 2, subject: "Mathématiques", title: "Addition et soustraction des fractions", type: "Exercice corrigé", text: "Pour des dénominateurs identiques, additionnez les numérateurs puis gardez le dénominateur." }, { id: 3, subject: "Anglais", title: "Les verbes irréguliers", type: "Fiche de révision", text: "Révisez les formes : be / was-were / been, go / went / gone, have / had / had." }];
export default function LearningWorkspacePage() { const [subject, setSubject] = useState("Mathématiques"); const [opened, setOpened] = useState(null); const list = resources.filter((r) => r.subject === subject); return <div className="min-h-screen bg-[#FAF9FB] font-sans"><AppHeader links={["Mes enfants", "Paiements", "Litiges"]} /><main className="mx-auto max-w-5xl px-4 py-8 sm:px-8"><p className="text-xs font-medium text-pf-purple">Espace d’apprentissage</p><h1 className="mt-1 font-serif text-2xl text-pf-purple-dark">Cours de Yannick</h1><div className="mt-7 grid gap-5 md:grid-cols-[1fr_2fr]"><aside className="rounded-xl border bg-white p-5"><h2 className="font-serif text-lg text-pf-purple-dark">Matières</h2>{["Mathématiques", "Anglais"].map((name) => <button key={name} onClick={() => setSubject(name)} className={`mt-3 flex w-full justify-between rounded-md px-3 py-2.5 text-left text-sm ${subject === name ? "bg-pf-purple-light text-pf-purple-dark" : "text-gray-600"}`}>{name}<span>{resources.filter((r) => r.subject === name).length}</span></button>)}</aside><section className="rounded-xl border bg-white p-5"><div className="flex justify-between"><h2 className="font-serif text-lg text-pf-purple-dark">{subject}</h2><Pill tone="purple">Lecture seule</Pill></div>{list.map((item) => <button key={item.id} onClick={() => setOpened(item)} className="flex w-full items-center gap-3 border-b py-4 text-left last:border-0"><span className="rounded-lg bg-pf-purple-light p-2"><FileText className="h-5 w-5 text-pf-purple" /></span><span className="flex-1"><b className="block text-sm text-pf-purple-dark">{item.title}</b><small className="text-gray-500">{item.type}</small></span><ChevronRight className="h-5 w-5 text-gray-400" /></button>)}</section></div></main>{opened && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><article className="w-full max-w-xl rounded-xl bg-white p-6"><button className="float-right" onClick={() => setOpened(null)}><X /></button><BookOpen className="h-5 w-5 text-pf-purple" /><p className="mt-3 text-xs text-pf-purple">{opened.type}</p><h2 className="mt-1 font-serif text-xl text-pf-purple-dark">{opened.title}</h2><p className="mt-5 text-sm leading-relaxed text-gray-600">{opened.text}</p></article></div>}</div>; }
