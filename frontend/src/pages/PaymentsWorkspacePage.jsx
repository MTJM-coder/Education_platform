import { useState } from "react";
import { ArrowLeft, CreditCard, X } from "lucide-react";
import AppHeader from "../components/layout/AppHeader";
import { Pill, StatCard } from "../components/ui/StatusUi";
const history = [{ ref: "PAY-2024-091", item: "Yannick · Mathématiques", date: "16 septembre", amount: "12 000 FCFA", status: "En séquestre", tone: "gold" }, { ref: "PAY-2024-074", item: "Yannick · Mathématiques", date: "30 août", amount: "12 000 FCFA", status: "Libéré", tone: "green" }];
export default function PaymentsWorkspacePage() { const [open, setOpen] = useState(false); const [success, setSuccess] = useState(false); return <div className="min-h-screen bg-[#FAF9FB] font-sans"><AppHeader links={["Mes enfants", "Paiements", "Litiges"]} /><main className="mx-auto max-w-5xl px-4 py-8 sm:px-8"><a href="/parent-dashboard" className="inline-flex items-center gap-1 text-sm text-gray-500"><ArrowLeft className="h-4 w-4" /> Retour au tableau de bord</a><div className="mt-5 flex items-end justify-between"><div><h1 className="font-serif text-2xl text-pf-purple-dark">Paiements</h1><p className="mt-1 text-sm text-gray-600">Suivez vos paiements et leur séquestre.</p></div><button onClick={() => {setOpen(true);setSuccess(false)}} className="flex items-center gap-2 rounded-md bg-pf-purple px-4 py-2.5 text-sm text-white"><CreditCard className="h-4 w-4" /> Initier un paiement</button></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><StatCard label="En séquestre" value="12 000 FCFA" /><StatCard label="Paiements ce mois" value="2" /><StatCard label="Total réglé" value="32 000 FCFA" /></div><section className="mt-7 overflow-hidden rounded-xl border bg-white"><h2 className="border-b p-5 font-serif text-lg text-pf-purple-dark">Historique des paiements</h2>{history.map((payment) => <div key={payment.ref} className="flex flex-wrap items-center justify-between gap-3 border-b p-5 last:border-0"><div><b className="block text-sm text-pf-purple-dark">{payment.item}</b><small className="text-gray-500">{payment.ref} · {payment.date}</small></div><b className="text-sm">{payment.amount}</b><Pill tone={payment.tone}>{payment.status}</Pill></div>)}</section></main>{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><div className="w-full max-w-md rounded-xl bg-white p-6"><button onClick={() => setOpen(false)} className="float-right"><X /></button><h2 className="font-serif text-xl text-pf-purple-dark">Initier un paiement</h2>{success ? <p className="mt-5 text-sm text-pf-green">Paiement enregistré. Le montant restera en séquestre jusqu’à confirmation de la séance.</p> : <><label className="mt-5 block text-sm">Enfant<select className="mt-1 w-full rounded-md border p-2"><option>Yannick</option></select></label><label className="mt-4 block text-sm">Forfait<select className="mt-1 w-full rounded-md border p-2"><option>2 séances · 12 000 FCFA</option></select></label><label className="mt-4 block text-sm">Mode de paiement<select className="mt-1 w-full rounded-md border p-2"><option>Mobile Money</option><option>Carte bancaire</option></select></label><button onClick={() => setSuccess(true)} className="mt-5 rounded-md bg-pf-purple px-4 py-2 text-sm text-white">Confirmer le paiement</button></>}</div></div>}</div>; }
